Für die ESP8266 mit einem ds18b20 Temperatursensor.

ESP misst, sendet per WLAN an eine InfluxDB und geht in den Deepsleep.

Für Deepsleep muss ein 470R Widerstand zwischen D0 und RST gelötet werden.